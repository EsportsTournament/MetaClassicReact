import React, { FC } from "react";
import "./Menubar.scss";

const Menubar: FC = () => {
  return (
    <div className="" data-testid="Menubar">
      This is menubar
    </div>
  );
};

export default Menubar;
